import java.util.*;
class hashset{
    public static void main(String[] args) {
        HashSet<Integer> hs=new HashSet<>();
        // add element
        hs.add(10);
        // size of the set
        hs.size();
        hs.isEmpty();
        hs.clear();
        hs.isEmpty();
    }
}